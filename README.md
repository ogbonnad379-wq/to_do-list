# To-Do List

A minimal, cross-platform to‑do list application for keeping tasks organized. This repository contains the source for a simple task manager that supports creating, editing, completing, and removing tasks.

## Features
- Add, edit, and delete tasks
- Mark tasks complete/incomplete
- Persist tasks locally (localStorage or filesystem depending on implementation)
- Responsive, accessible UI (intended).